---
title: "Estante Gaveteiro cinza 36 gavetas padrão 30cm Altura 198cm"
category: "Estantes Gaveteiros"
height: "198 cm"
width: "92 cm"
depth: "30 cm"
sheetWidth: "24 e 22"
items:
  [
    "Acompanha: 36 gavetas",
    "Material: Aço",
    "Pintura: Eletrostática Texturizada",
    "Temos disponível no colorido e outras profundidades",
    "Disponível na chapa 24 e 22",
  ]
heroImage: "./1.png"
---

import { Image } from "astro:assets";
import ProductCarousel from "../../../components/ProductCarousel.astro";

import product1 from "./1.png";
import product2 from "./2.png";
import product3 from "./3.png";
import product4 from "./4.png";

export const TOTAL_PRODUCTS = 4;

export const imageList = [
  { src: product1 },
  { src: product2 },
  { src: product3 },
  { src: product4 },
];

<ProductCarousel>
  {imageList.map((image) => (
    <div class="glide__slide">
      <Image
        src={image.src}
        widths={[370, 500]}
        sizes="(max-width: 440px) 370px, 500px"
        alt={frontmatter.title}
        class="object-contain h-[500px] mx-auto p-5 mb-1 sm:mb-5"
      />
    </div>
  ))}

  <Fragment slot="bullets">
    {TOTAL_PRODUCTS > 1 && Array(TOTAL_PRODUCTS).fill(0).map((_, i) => <button class="glide__bullet" data-glide-dir={`=${i}`}></button>)}
  </Fragment>
</ProductCarousel>
