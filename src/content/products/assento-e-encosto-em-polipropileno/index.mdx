---
title: "Assento e Encosto em Polipropileno 100% Reciclável"
category: "Cadeiras e Assentos"
height: "470 mm"
width: "430 mm"
depth: "31 mm"
# sheetWidth: "26"
items:
  [
    "Encosto com medidas de 295 x 470 x 28 mm (altura x largura x espessura)",
    "Assento anatômico com medidas de 470 x 430 x 31 mm (largura x profundidade x espessura)",
    "Disponível para 2, 3, 4 e 5 lugares",
    "Código: 000273",
  ]
heroImage: "./1.png"
---

import { Image } from "astro:assets";
import ProductCarousel from "../../../components/ProductCarousel.astro";

import product1 from "./1.png";
import product2 from "./2.png";

export const TOTAL_PRODUCTS = 2;

export const imageList = [{ src: product1 }, { src: product2 }];

<ProductCarousel>
  {imageList.map((image) => (
    <div class="glide__slide">
      <Image
        src={image.src}
        widths={[370, 500]}
        sizes="(max-width: 440px) 370px, 500px"
        alt={frontmatter.title}
        class="object-contain h-[500px] mx-auto p-5 mb-1 sm:mb-5"
      />
    </div>
  ))}

  <Fragment slot="bullets">
    {TOTAL_PRODUCTS > 1 && Array(TOTAL_PRODUCTS).fill(0).map((_, i) => <button class="glide__bullet" data-glide-dir={`=${i}`}></button>)}
  </Fragment>
</ProductCarousel>
