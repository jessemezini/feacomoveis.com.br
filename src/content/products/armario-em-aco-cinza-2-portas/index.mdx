---
title: "Armário em aço cinza 2 portas, altura 170 cm"
category: "Armários"
height: "170 cm"
width: "90 cm"
depth: "40 cm"
sheetWidth: "26"
items:
  [
    "Bandeja central fixa, demais ajustáveis",
    "Suporta: 30 kg por bandeja",
    "Pintura: Eletrostática Texturizada",
    "Disponível na chapa 26, 24 e 22",
  ]
desc: "Temos também nas medidas:  Altura: 170, Largura: 90, Profundidade: 40. Consulte"
heroImage: "./1.png"
---

import { Image } from "astro:assets";
import ProductCarousel from "../../../components/ProductCarousel.astro";

import product1 from "./1.png";
import product2 from "./2.png";

export const TOTAL_PRODUCTS = 2;

export const imageList = [{ src: product1 }, { src: product2 }];

<ProductCarousel>
  {imageList.map((image) => (
    <div class="glide__slide">
      <Image
        src={image.src}
        widths={[370, 500]}
        sizes="(max-width: 440px) 370px, 500px"
        alt={frontmatter.title}
        class="object-contain h-[500px] mx-auto p-5 mb-1 sm:mb-5"
      />
    </div>
  ))}

  <Fragment slot="bullets">
    {TOTAL_PRODUCTS > 1 && Array(TOTAL_PRODUCTS).fill(0).map((_, i) => <button class="glide__bullet" data-glide-dir={`=${i}`}></button>)}
  </Fragment>
</ProductCarousel>
