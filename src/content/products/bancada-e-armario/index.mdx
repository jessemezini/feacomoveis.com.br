---
title: "Bancada com Armário em Aço"
category: "Bancadas e Balcões"
height: "100 cm"
width: "400 cm"
depth: "50 cm"
# sheetWidth: "26"
items:
  [
    "Fabricado em aço de alta durabilidade",
    "Pintura: eletrostática texturizada",
    "Produzido sob medida conforme a necessidade do ambiente",
    "Ideal para recepções, áreas de atendimento e ambientes corporativos",
    "Código: 000275",
  ]
heroImage: "./1.jpeg"
---

import { Image } from "astro:assets";
import ProductCarousel from "../../../components/ProductCarousel.astro";

import product1 from "./1.jpeg";

export const TOTAL_PRODUCTS = 1;

export const imageList = [{ src: product1 }];

<ProductCarousel>
  {imageList.map((image) => (
    <div class="glide__slide">
      <Image
        src={image.src}
        widths={[370, 500]}
        sizes="(max-width: 440px) 370px, 500px"
        alt={frontmatter.title}
        class="object-contain h-[500px] mx-auto p-5 mb-1 sm:mb-5"
      />
    </div>
  ))}

  <Fragment slot="bullets">
    {TOTAL_PRODUCTS > 1 && Array(TOTAL_PRODUCTS).fill(0).map((_, i) => <button class="glide__bullet" data-glide-dir={`=${i}`}></button>)}
  </Fragment>
</ProductCarousel>
